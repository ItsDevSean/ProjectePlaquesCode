/*! Oruga v0.9.3 | MIT License | github.com/oruga-ui/oruga */
import { ref, toRaw } from "vue";
import { getValueByPath, setValueByPath, merge } from "./helpers.mjs";
import { s as setVueInstance } from "./plugins-CjY7EZio.mjs";
const globalOptions = ref({
  iconPack: "mdi",
  useHtml5Validation: true,
  statusIcon: true,
  transformClasses: void 0
});
const setOptions = (options) => {
  globalOptions.value = options;
};
const getOptions = () => {
  return Object.assign({}, toRaw(globalOptions.value));
};
const getOption = (path, defaultValue) => {
  return getValueByPath(globalOptions.value, path, defaultValue);
};
const getDefault = (path, defaultValue) => {
  return getValueByPath(globalOptions.value, path, defaultValue);
};
const setOption = (path, value) => {
  setValueByPath(globalOptions.value, path, value);
};
const ConfigProgrammatic = {
  getOption,
  getOptions,
  setOption,
  setOptions(options) {
    setOptions(merge(getOptions(), options, true));
  }
};
const OrugaConfig = {
  install(app, options) {
    setVueInstance(app);
    setOptions(merge(getOptions(), options, true));
  }
};
export {
  ConfigProgrammatic,
  OrugaConfig,
  getDefault,
  getOption,
  getOptions,
  setOption,
  setOptions
};
//# sourceMappingURL=config.mjs.map
