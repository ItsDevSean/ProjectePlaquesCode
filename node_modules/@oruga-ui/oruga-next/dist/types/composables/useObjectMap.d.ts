import { MaybeRefOrGetter } from 'vue';
export type ObjectMap<T> = Array<{
    key: string | number;
    value: T;
}>;
/** wrap entities in an array of objects with key/value attributes */
export declare function useObjectMap<T>(values: MaybeRefOrGetter<Array<T>> | undefined, key: string | number | symbol | undefined, uuid: () => string): ObjectMap<T>;
